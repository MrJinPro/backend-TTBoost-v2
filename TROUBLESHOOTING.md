# Диагностика проблемы "Не получаю события из TikTok Live"

## Проверка 1: Указан ли TikTok username?

1. Откройте приложение в браузере
2. Перейдите на вкладку "Настройки" (Settings)
3. Проверьте, заполнено ли поле "TikTok username"
4. Если НЕТ - введите username стримера (БЕЗ @, например: `mystreamer`)
5. Нажмите "Сохранить"

## Проверка 2: Запущен ли Live стрим?

1. Откройте TikTok в браузере
2. Перейдите на профиль `@mystreamer`
3. Убедитесь что есть LIVE трансляция (красный значок LIVE)
4. Если стрим не в эфире - события НЕ БУДУТ приходить

## Проверка 3: Подключается ли WebSocket?

1. Откройте DevTools (F12) -> вкладка Console
2. Нажмите кнопку "Подключить" в приложении
3. Смотрите логи в консоли:
   - `WS: Connecting to wss://api.ttboost.pro/v2/ws?token=...` - началось подключение
   - `WS: Connected successfully` - подключено
   - `WS: Received message: {type: "status", message: "Подключено к TikTok Live @...", connected: true}` - TikTok клиент запущен

### Если видите ошибку:

**`WS error: WebSocketChannelException: WebSocketChannelException: HttpException: Connection closed before full header was received`**
- Проблема с сетью или сервер недоступен

**`WS: Received message: {type: "error", message: "TikTok username не указан..."}`**
- Вернитесь к Проверке 1

**`WS: Received message: {type: "error", message: "Ошибка подключения к TikTok Live: ..."}`**
- Ошибка при подключении к TikTok (см. детали в сообщении)

## Проверка 4: Задеплоена ли последняя версия на сервере?

Проверьте что на сервере api.ttboost.pro находится последняя версия кода с поддержкой `/v2/ws`.

### Как проверить:

```bash
# На сервере api.ttboost.pro
cd /opt/ttboost  # или где у вас backend
git log -1 --oneline
# Должен быть коммит с trigger_name, combo_count, gifts_library.json
```

Если коммита нет - сделайте `git pull` и рестарт сервиса.

## Проверка 5: Настроены ли TIKTOK_COOKIES?

Для получения **подарков** (GiftEvent) в TikTok Live нужны cookies авторизованного пользователя.

### Проверка:

```bash
# На сервере
cat .env | grep TIKTOK_COOKIES
```

Если пусто - настройте:

1. Авторизуйтесь на tiktok.com в браузере
2. Откройте DevTools -> Application -> Cookies -> https://www.tiktok.com
3. Скопируйте ВСЕ cookies в формате `name1=value1; name2=value2; ...`
4. Добавьте в `.env`:
   ```
   TIKTOK_COOKIES="sessionid=xxx; tt_chain_token=yyy; ..."
   ```
5. Рестарт сервиса

**БЕЗ COOKIES**: Комментарии и лайки работают, но подарки НЕ ПРИХОДЯТ.

## Проверка 6: Используйте тестовый скрипт

```bash
# На вашем компьютере
cd D:\Projects\ttboost-mobile\backend
python test_ws_connection.py
```

Скрипт покажет:
- Подключается ли WebSocket
- Какие события приходят
- Ошибки подключения

### Как использовать:

1. Получите JWT токен:
   - Откройте приложение в браузере
   - DevTools (F12) -> Console
   - Выполните: `localStorage.getItem('jwtToken')`
   - Скопируйте токен

2. Откройте `test_ws_connection.py`

3. Замените `YOUR_JWT_TOKEN_HERE` на ваш токен

4. Запустите:
   ```bash
   python test_ws_connection.py
   ```

5. Откройте TikTok Live стрим в другой вкладке

6. Напишите комментарий, отправьте подарок - должны появиться события в консоли

## Проверка 7: Логи на сервере

```bash
# На сервере
tail -f /var/log/ttboost.log  # или где у вас логи
# или
journalctl -u ttboost -f  # если через systemd
```

Ищите:
- `TikTok Live подключен: @username` - успешное подключение
- `TikTok комментарий от ...` - получен комментарий
- `🎁 GiftEvent получен` - получен подарок
- `Watchdog: нет активности` - нет событий долгое время (проверьте стрим)

## Частые проблемы:

### 1. "Нет событий вообще"
- Стрим не в эфире
- TikTok username не указан или неправильный
- WebSocket не подключен

### 2. "Есть комментарии, но нет подарков"
- Не настроены TIKTOK_COOKIES
- Используется free tier Sign API (ограничения)
- Никто не отправляет подарки (проверьте отправив сами с телефона)

### 3. "События приходят, но не проигрываются"
- Проверьте настройки TTS / Gift Sounds (включены ли)
- Проверьте что звуковые файлы загружены для триггеров
- Откройте DevTools -> Network -> фильтр `.mp3` - загружаются ли файлы

### 4. "WebSocket постоянно переподключается"
- Ошибка на сервере при обработке событий
- Проблемы с TikTok API (rate limit, signature errors)
- Смотрите логи сервера

## Итоговый чеклист:

- [ ] TikTok username указан в настройках профиля
- [ ] Live стрим ИДЕТ В ЭФИРЕ прямо сейчас
- [ ] WebSocket подключен (зеленая кнопка "Отключить")
- [ ] В консоли видно `Подключено к TikTok Live @username`
- [ ] На сервере последняя версия кода (с /v2/ws)
- [ ] На сервере настроены TIKTOK_COOKIES (для подарков)
- [ ] В логах сервера видно подключение к TikTok
- [ ] Тестовый скрипт показывает события

Если все галочки стоят, но события НЕ приходят:
- Отправьте логи с сервера (последние 200 строк)
- Покажите скриншот консоли браузера
- Убедитесь что кто-то пишет комментарии в Live стриме (или напишите сами с телефона)
