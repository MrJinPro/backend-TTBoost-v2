# 🧪 План тестирования TTBoost Mobile v2

## После установки Flutter

### 1. Проверка установки
```powershell
# ПЕРЕЗАПУСТИТЕ PowerShell!
flutter --version
flutter doctor
```

### 2. Установка зависимостей
```powershell
cd D:\Projects\ttboost-mobile\mobile
flutter pub get
```

### 3. Запуск приложения (Chrome - самый быстрый вариант)
```powershell
flutter run -d chrome --web-renderer html
```

---

## Тестовый сценарий

### Шаг 1: Активация лицензии (первый запуск)

**Данные для активации:**
- Лицензионный ключ: `TTB-9E2E-5DE1-A3FC`
- Имя пользователя: `testuser_new` (придумайте свой)
- Пароль: `MySecurePass123!`

**Что проверяем:**
- ✅ Форма активации открывается
- ✅ Ввод всех 3 полей работает
- ✅ Кнопка "Активировать лицензию" работает
- ✅ После активации переход на главный экран
- ✅ JWT токен сохраняется (при следующем запуске автовход)

### Шаг 2: Настройки профиля

**Откройте Settings (шестерёнка в правом верхнем углу)**

**Что настроить:**
- TikTok Username: `ваш_тикток_username` (ОБЯЗАТЕЛЬНО!)
- Voice: `ru-RU-SvetlanaNeural` (женский) или `ru-RU-DmitryNeural` (мужской)
- TTS Volume: 80-100
- Gifts Volume: 80-100

**Что проверяем:**
- ✅ Настройки сохраняются
- ✅ API запрос `/v2/settings/update` успешен

### Шаг 3: Загрузка звука

**Подготовьте звуковой файл:**
- Формат: MP3
- Размер: ≤100 KB
- Длительность: ≤5 секунд

**Действия:**
1. Откройте "Gift Sounds" или "Upload Sound"
2. Выберите файл
3. Загрузите

**Что проверяем:**
- ✅ File picker открывается
- ✅ Файл загружается без ошибок
- ✅ Звук появляется в списке
- ✅ URL звука: `https://media.ttboost.pro/sounds/...`

### Шаг 4: Создание триггера

**Создайте триггер для подарка Rose:**
- Event Type: `gift`
- Condition: `gift_name = Rose`
- Action: `play_sound` или `tts`
- Sound/Text: выберите загруженный звук или текст "Спасибо за розу, {user}!"

**Что проверяем:**
- ✅ Триггер создаётся
- ✅ Появляется в списке триггеров
- ✅ Можно включить/выключить
- ✅ Можно удалить

### Шаг 5: Подключение к стриму

**ВАЖНО: Нужен активный TikTok Live стрим!**

**Действия:**
1. Убедитесь что TikTok username установлен
2. Нажмите кнопку "Connect" на главном экране
3. Дождитесь статуса "Connected"

**Что проверяем:**
- ✅ WebSocket подключается: `wss://api.ttboost.pro/v2/ws?token=...`
- ✅ Статус меняется на "Connected" (зелёный)
- ✅ Нет ошибок подключения

### Шаг 6: Получение событий

**Во время стрима попросите кого-то:**
- Написать комментарий
- Отправить подарок Rose
- Поставить лайк

**Что проверяем:**
- ✅ События приходят в WebSocket
- ✅ Комментарии озвучиваются (TTS)
- ✅ Подарки воспроизводят звуки
- ✅ События логируются на экране
- ✅ Триггеры срабатывают

---

## Проверка автоматических функций

### Auto-login
1. Закройте приложение
2. Откройте снова
3. ✅ Должен автоматически войти (без ввода данных)

### JWT Refresh
1. Подождите 24 часа (или измените TTL на сервере)
2. Откройте приложение
3. ✅ Должен автоматически обновить токен (re-login)

### Offline Mode
1. Отключите интернет
2. Откройте приложение
3. ✅ Настройки должны загрузиться из кеша
4. ⚠️ API запросы будут недоступны

---

## Известные проблемы

### Web (Chrome)
- ❌ File picker может не работать из-за CORS
- ✅ Решение: используйте Desktop (Windows) версию

### Windows Desktop
- ⚠️ Flutter Secure Storage требует дополнительной настройки
- ✅ Решение: данные сохранятся в SharedPreferences как fallback

### Android
- ⚠️ Требуются разрешения Storage и Internet
- ✅ Добавлены в AndroidManifest.xml

---

## Команды для дебага

### Просмотр логов
```powershell
flutter logs
```

### Очистка кеша
```powershell
flutter clean
flutter pub get
```

### Пересборка
```powershell
flutter build web --release
flutter build windows --release
```

### Проверка API вручную
```powershell
# Вход
$body = @{ username = "testuser_new"; password = "MySecurePass123!" } | ConvertTo-Json
$response = Invoke-RestMethod -Uri "https://api.ttboost.pro/v2/auth/login" -Method POST -ContentType "application/json" -Body $body
$token = $response.access_token

# Профиль
Invoke-RestMethod -Uri "https://api.ttboost.pro/v2/auth/me" -Headers @{ Authorization = "Bearer $token" }
```

### Gift transport debug (RAW Webcast probe)
Для проверки, приходят ли подарки на транспортном уровне от TikTok:

1) Установите переменные окружения (если используете EulerStream):
   - SIGN_API_KEY
   - SIGN_API_URL (или SIGN_SERVER_URL)

2) Запустите probe-скрипт на Python (требует зависимости из backend/requirements.txt):

```powershell
cd D:\Projects\ttboost-mobile\backend
python -m venv .venv; .venv\Scripts\Activate.ps1
pip install -r requirements.txt

# Логирование типов RAW-сообщений и детекция подарков (по строке типа)
python .\tools\raw_gift_probe.py <tiktok_username>
```

Ожидаемое поведение:
- В консоль выводятся строки вида: `[probe] frame types: {'WebcastChatMessage': 3, 'WebcastLikeMessage': 1}`
- При наличии подарков будет строка: `[probe] GIFT messages detected in frame: <N>`
- Если за 60 секунд подарков нет и типы `...GiftMessage` не встречаются — высока вероятность, что анонимная сессия/бесплатный ключ не передаёт подарки.

Подсказки:
- Для стабильного получения подарков используйте авторизованные cookie/премиум-ключ подписи.
- Если подарков нет, но комментарии/лайки приходят — это не проблема парсинга, а фильтрация на стороне источника.

---

## Критерии успеха

### ✅ Приложение работает если:
1. Вход/активация проходит успешно
2. Настройки сохраняются
3. Звуки загружаются
4. Триггеры создаются
5. WebSocket подключается
6. События приходят и обрабатываются

### ❌ Приложение НЕ работает если:
1. Ошибки авторизации (JWT invalid)
2. WebSocket не подключается
3. События не приходят
4. TTS/звуки не воспроизводятся
5. Настройки не сохраняются

---

## Следующие шаги после успешного теста

1. **Собрать релизную версию:**
   ```powershell
   flutter build windows --release
   flutter build web --release
   flutter build apk --release  # для Android
   ```

2. **Развернуть web-версию:**
   - Скопировать `build/web/*` на хостинг
   - Настроить HTTPS
   - Настроить CORS

3. **Распространить Windows версию:**
   - Найти EXE в `build/windows/runner/Release/`
   - Создать инсталлятор (опционально)

4. **Опубликовать в Google Play / App Store:**
   - Следовать гайдам Flutter
   - Подготовить скриншоты и описание
